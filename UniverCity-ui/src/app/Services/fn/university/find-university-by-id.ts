/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UniversityResponse } from '../../models/university-response';

export interface FindUniversityById$Params {
  'university-id': number;
}

export function findUniversityById(http: HttpClient, rootUrl: string, params: FindUniversityById$Params, context?: HttpContext): Observable<StrictHttpResponse<UniversityResponse>> {
  const rb = new RequestBuilder(rootUrl, findUniversityById.PATH, 'get');
  if (params) {
    rb.path('university-id', params['university-id'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UniversityResponse>;
    })
  );
}

findUniversityById.PATH = '/university/{university-id}';
